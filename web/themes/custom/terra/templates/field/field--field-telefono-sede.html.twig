{%
  set classes = [
    'field',
    'field--name-' ~ field_name|clean_class,
  ]
%}

<div{{ attributes.addClass(classes) }}>
    {% for item in items %}
       <div class="field__label">{{'Teléfono'|t}}</div>
       <div class="field__item"><a href="tel:+34{{item.content}}" title="{{ item.content }}">{{ item.content }}</a></div>
    {% endfor %}
</div>